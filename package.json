{
  "name": "tempo-ai-text-call",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "syntaxcheck": "node --check public/app.js && node --check public/sse.js",
    "test": "node --test",
    "check": "npm run typecheck && npm run syntaxcheck && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.33.1"
  }
}
